---
phase: 4
plan: 1
wave: 1
---

# Plan 4.1: UI Theme & Layout Foundation

## Objective
Implement the Royal Edition (Haleon dark-green) theme tokens across Tailwind and global CSS, and rebuild the main Dashboard Layout shell (Sidebar + Header) based on the `dashboard_royal_edition` template.

## Context
- .gsd/SPEC.md
- stitch/dashboard_royal_edition/code.html
- frontend/tailwind.config.js
- frontend/src/styles/index.css
- frontend/src/components/Layout.tsx

## Tasks

<task type="auto">
  <name>Apply Royal Edition Tailwind Tokens</name>
  <files>frontend/tailwind.config.js, frontend/src/styles/index.css</files>
  <action>
    Extract the raw hex colors from `stitch/dashboard_royal_edition/code.html` tailwind config block:
    - `"primary": "#10b77f"`, `"background-light": "#f6f8f7"`, `"background-dark": "#080c0a"`, `"emerald-dark": "#0a1a14"`, `"emerald-card": "#11221c"`
    - Enforce "Inter" as the primary display font.
    - Set the background and text color utility classes inside `body` directly via tailwind directives in `index.css`.
  </action>
  <verify>npm run build in frontend</verify>
  <done>Tailwind compiles and `frontend/tailwind.config.js` contains the exact Royal Edition hex tokens.</done>
</task>

<task type="auto">
  <name>Build Main Dashboard Layout</name>
  <files>frontend/src/components/Layout.tsx</files>
  <action>
    Convert the HTML side navigation (`<aside>`) and top header (`<header>`) from `stitch/dashboard_royal_edition/code.html` into a reusable React layout component.
    - Replace the Material Icons with `lucide-react` equivalents where sensible, or embed Google Material Symbols as linked in the template.
    - Ensure React Router `<Outlet />` renders inside the `<main>` tag.
    - Apply the exact `bg-emerald-dark border-r border-primary/10` and `bg-background-dark/50 backdrop-blur-md` classes as provided.
  </action>
  <verify>Check dev server rendering for Layout.tsx</verify>
  <done>Layout.tsx structure perfectly matches the Haleon template design visually.</done>
</task>

## Success Criteria
- [ ] Tailwind tokens strictly map the royal edition palette.
- [ ] Next phase can drop page components seamlessly into the new `<Layout />`.
