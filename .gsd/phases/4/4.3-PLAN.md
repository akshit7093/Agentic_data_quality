---
phase: 4
plan: 3
wave: 2
---

# Plan 4.3: Rules, Validations & Settings UI

## Objective
Convert the final sets of pages (Validations Empty state, Rule Groups listing, and LLM Provider Settings) using the respective `_royal_edition` templates.

## Context
- stitch/validations_empty_royal_edition/code.html
- stitch/rule_groups_royal_edition/code.html
- stitch/settings_royal_edition/code.html
- frontend/src/pages/Validations.tsx
- frontend/src/pages/Rules.tsx
- frontend/src/pages/Settings.tsx

## Tasks

<task type="auto">
  <name>Validations (Empty State) Overhaul</name>
  <files>frontend/src/pages/Validations.tsx</files>
  <action>
    Port the empty state design from `stitch/validations_empty_royal_edition/code.html`.
    - Ensure the "No Validations Yet" placeholder contains the stylized buttons (`Create First Validation` and `View Templates`).
  </action>
  <verify>Check Validations.tsx rendering</verify>
  <done>Empty state perfectly matches the template.</done>
</task>

<task type="auto">
  <name>Rule Groups UI Conversion</name>
  <files>frontend/src/pages/Rules.tsx</files>
  <action>
    Port the Rule Groups UI from `stitch/rule_groups_royal_edition/code.html`.
    - Implement the main "bmc Validation Engine" featured card with neon sweeps (or exact CSS mapped equivalent).
    - Migrate the smaller grid cards (Database Consistency, Cloud API Schema) focusing on structure and hover interactions.
  </action>
  <verify>Check Rules.tsx rendering</verify>
  <done>Rule Groups page resembles the dark-themed reference.</done>
</task>

<task type="auto">
  <name>LLM Provider Settings Styling</name>
  <files>frontend/src/pages/Settings.tsx</files>
  <action>
    Bring over the form controls and provider selector logic from `stitch/settings_royal_edition/code.html`.
    - Match the horizontal layout for "LLM Configuration, Database, Notifications, Security".
    - Replace default inputs with the `bg-[rgba(...)]` borders and text paddings specifically designed for the Royal theme.
  </action>
  <verify>Check Settings.tsx rendering</verify>
  <done>Settings page fully utilizes the new inputs and layout paddings.</done>
</task>

## Success Criteria
- [ ] Entire application accurately reflects the Haleon Royal Edition Theme without leaking old Tailwind colors.
