---
phase: 4
plan: 2
wave: 1
---

# Plan 4.2: Dashboard & Data Sources Conversion

## Objective
Re-skin the existing Dashboard and Data Sources views to replicate the `dashboard_royal_edition` and `data_sources_royal_edition` HTML templates perfectly.

## Context
- stitch/dashboard_royal_edition/code.html
- stitch/data_sources_royal_edition/code.html
- frontend/src/pages/Dashboard.tsx
- frontend/src/pages/DataSources.tsx

## Tasks

<task type="auto">
  <name>Dashboard Overview Metric Cards & Tables</name>
  <files>frontend/src/pages/Dashboard.tsx</files>
  <action>
    Replace the internal content of `Dashboard.tsx` with the grid metrics, active validations table, and system status sidebar from `stitch/dashboard_royal_edition/code.html`.
    - Map the 4 top summary cards (Total Validations, Data Sources, Validation Rules, Avg Quality Score) to dynamic state data placeholders.
    - Implement the "Recent Validations" table with the correct progress bars (`w-24 h-1.5 bg-slate-800 rounded-full`).
    - Use lucide/material icons as specified.
  </action>
  <verify>Check Dashboard.tsx rendering</verify>
  <done>The React rendered Dashboard is visually indistinguishable from the provided Dashboard HTML.</done>
</task>

<task type="auto">
  <name>Data Sources Page UI</name>
  <files>frontend/src/pages/DataSources.tsx</files>
  <action>
    Redesign the Data Sources page using `stitch/data_sources_royal_edition/code.html`.
    - Create the upper nav tabs (ALL SOURCES, ACTIVE, etc.).
    - Implement the Source Cards (`bg-emerald-card border border-primary/10 p-6 rounded-xl`) with respective statuses (e.g. 98% Clean, 42 Files Indexed).
    - Port the "Automate with API" CLI snippet footer to react.
  </action>
  <verify>Check DataSources.tsx rendering</verify>
  <done>The Data Sources page fully implements the Royal Edition styling and grid structure.</done>
</task>

## Success Criteria
- [ ] Dashboard shows the dark/neon green metrics logic.
- [ ] Data Sources displays connections as robust Royal Edition tiles.
